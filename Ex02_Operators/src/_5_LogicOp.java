// 논리 연산자
// && (and), || (or)
// && (and) : 둘 다 true일 때 결과가 true
// || (or)  : 둘 중에 1개가 true면 결과는 true
public class _5_LogicOp {
	public static void main(String[] args) {
		boolean bX = true;
		boolean bY = false;
		System.out.println(bX && bY);
		System.out.println(bX || bY);
	}
}
